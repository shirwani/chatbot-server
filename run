#python3 -m venv venv

#source venv/bin/activate
#alias python="venv/bin/python3"
#alias pip="venv/bin/pip3"

#pip install --upgrade pip
#pip install -r requirements.txt

# scp ../clothes.csv root@173.255.238.242:/var/www/flask-apps/.


# ON REMOTE SERVER


# gunicorn -b 0.0.0.0:8001 chat_server:app # to test out the server without systemd

#sudo mkdir -p /var/www/.cache
#sudo chown -R www-data:www-data /var/www/.cache
#sudo find /var/www/.cache -type d -exec chmod 755 {} +
#sudo find /var/www/.cache -type f -exec chmod 644 {} +


#sudo systemctl daemon-reload
#sudo systemctl enable --now chatbot-server

#fuser -k 8001/tcp

sudo systemctl daemon-reload
sudo systemctl stop chatbot-server
sudo systemctl reset-failed chatbot-server

sudo systemctl restart chatbot-server
sudo systemctl status chatbot-server --no-pager
sudo journalctl -u chatbot-server -f



#git fetch origin
#git reset --hard origin/main
#git clean -fd

